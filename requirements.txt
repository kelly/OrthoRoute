# Core dependencies
numpy>=1.20.0
psutil>=5.8.0
scipy>=1.7.0
kicad-python>=0.5.0

# GUI dependencies (optional)
PyQt6>=6.0.0

# GPU acceleration - choose ONE based on your platform:
#
# For Apple Silicon (M1/M2/M3 Macs):
#   pip install mlx
#
# For NVIDIA GPUs (Linux/Windows):
#   pip install cupy-cuda11x  (or cupy-cuda12x for CUDA 12)
#
# Note: CuPy is NOT supported on macOS. Use MLX instead.

# Development dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.0.0
black>=21.0.0
flake8>=3.8.0
mypy>=0.800

# Documentation dependencies (optional)
sphinx>=4.0.0
sphinx-rtd-theme>=0.5.0
